// 4 - Раскрывающийся список
// Нужно сделать так, чтобы по нажатию на название списка (li), он раскрывался (класс tree__sub-tree_open) и закрывался

// const treeRef = document.querySelector('.tree');

// treeRef.addEventListener('click', e => {
//   const targetEl = e.target;

//   // if (targetEl.tagName !== 'LI' || !targetEl.children.length) {
//   //   return;
//   // }
//   // targetEl.firstElementChild.classList.toggle('tree__sub-tree_open');

//   if (targetEl.tagName !== 'LI') {
//     return;
//   }

//   targetEl.firstElementChild?.classList.toggle('tree__sub-tree_open');
// });
